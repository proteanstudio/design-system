// import { newSpecPage } from '@stencil/core/testing';
// import { ProteanSelect } from '.';

describe('protean-select', () => {
    it('renders', async () => {
        /* const { root } = await newSpecPage({
            components: [ProteanSelect],
            html: '<protean-select></protean-select>',
        }); */
    });
});
