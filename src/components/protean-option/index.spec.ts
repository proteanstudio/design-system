// import { newSpecPage } from '@stencil/core/testing';
// import { ProteanOption } from '.';

describe('protean-option', () => {
    it('renders', async () => {
        /* const { root } = await newSpecPage({
            components: [ProteanOption],
            html: '<protean-option></protean-option>',
        }); */
    });
});
